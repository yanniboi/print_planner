## #ddev-generated
## Custom web image additions for OpenSCAD CLI workflow

RUN apt-get update && DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends \
    openscad \
    xvfb \
    xauth \
    admesh \
    prusa-slicer \
    jq \
    && apt-get clean \
    && rm -rf /var/lib/apt/lists/*
